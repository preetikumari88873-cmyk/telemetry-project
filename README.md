# telemetry-project
This project converts telemetry data from multiple JSON formats into a unified output format using Python.  It reads input telemetry files, maps the fields correctly, and converts ISO timestamps into milliseconds  since epoch. The program includes automated unit tests to verify that the converted output matches the  required unified format.
